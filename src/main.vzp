import parse
import utils

/* Obviously there's a lot to do here, such as IO */

let program = IO.read("../tests/precedence/program.vzp")

let result = parse(program)
if result.wellFormed then
    print(utils.stringifyAst(result.ast))
else
    print(result.error)
