let fibs = (x :: Int64) => /* A comment here */
    if x < 2/*A comment here too */then
        1
    else
        recur(x - 1) + recur(x - 2)/*Comment*/

if fibs(5) == 8 then print("8") else print("bad")
